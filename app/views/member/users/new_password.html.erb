<%= page_title("Create Password") %>
<%= page_header "Create Password" %>

<%= render 'shared/users/profile_tab', current_index: 3 %>
<%= simple_form_for([:member, current_user], url: member_create_password_path, method: 'POST', html: { class: 'form form-lg form-inline'} ) do |f| %>
    <%= input_row do %>
      <%= f.input :password, as: :password, required: true,
                  input_html: { class: 'form-control', autocomplete: :off,} %>
    <%end%>

        <%= input_row do %>
      <%= f.input :password_confirmation, as: :password, required: true,
                  input_html: { class: 'form-control', autocomplete: :off} %>
    <%end%>

    <%= input_row do %>
      <label> </label>
      <%= f.button :submit, "Update", class: 'btn btn-primary' %>
    <%end%>

<% end %>


