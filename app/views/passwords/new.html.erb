<%= page_title "Forgot password" %>
<%= page_header "Forgot password" %>

<blockquote>
  If you have forgotten the username and password to your account, simply enter you email address you have registered with us and click the 
  <strong> Reset Password </strong> button. We will send an email to you that contains information on how to change the password.
</blockquote>

<%= simple_form_for(@password_form, url: passwords_path,html: { class: 'form form-inline'}) do |f| %>
  <%= input_row do %>
    <%=f.input :email, as: :string, input_html: {class: 'form-control'} %>
  <% end %>

  <%= input_row do %>
    <button class='btn btn-info btn-block'> Reset Password</button>
  <% end %>
<% end %>