<script>
  window.Config = {
    fbId: '<%= ENV["FB_ID"] %>',
    Flash: {
      key: '<%=flash_config[:key] %>',
      value: '<%= flash_config[:value] %>'
    }
  }

  window.Constant = {
    rejectReason: '<%= Property::VERIFICATION_STATUS_REJECT %>'
  }
</script>